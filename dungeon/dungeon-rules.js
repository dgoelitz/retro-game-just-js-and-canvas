import { handleDungeon0RoomEntry, updateDungeon0RoomRules } from "../dungeons/dungeon-0/dungeon-0-rules.js";

export function handleDungeonRoomEntry(session, ctx, canvas) {
  handleDungeon0RoomEntry(session, ctx, canvas);
}

export function updateDungeonRoomRules(session, ctx, canvas) {
  updateDungeon0RoomRules(session, ctx, canvas);
}
